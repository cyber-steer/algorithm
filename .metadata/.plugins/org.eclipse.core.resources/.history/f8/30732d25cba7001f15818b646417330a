package str;

import java.util.Scanner;

public class GetLength {
	public static void main(StringFirstEnd[] args) {
		Scanner scanner = new Scanner(System.in);
		StringFirstEnd s = scanner.nextLine();
		System.out.println(s.length());
		scanner.close();
	}
}
